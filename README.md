## 📦 Requirements
- Python 3.9+
- Libraries:
  - `pyttsx3`
  - `speech_recognition`
  - `psutil`
  - `pywhatkit`
  - `requests`
  - `shutil`
  - and others...

Install using:
```bash
pip install -r requirements.txt
